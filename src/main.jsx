import React from "react";
import { createRoot } from "react-dom/client";
import PolicyPulseExperimental from "../PolicyPulseExperimental.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PolicyPulseExperimental />
  </React.StrictMode>
);
